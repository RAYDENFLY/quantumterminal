# Optional: CoinGecko API for enhanced market data
COINGECKO_API_KEY=

# MongoDB Configuration
MONGODB_URI=
MONGODB_DB=

#GROQ API
GROQ_API_KEY=

# ImgBB Configuration (optional)
IMGBB_API_KEY=

# Quantum Trade (local / external backend)
# Used by Next.js API proxies under /api/quantum-trade/*
# Default is http://127.0.0.1:8000 when not set.
QUANTUM_TRADE_API_BASE_URL=
# If you want it visible in the browser (not required for the proxy routes), you can also set:
NEXT_PUBLIC_QUANTUM_TRADE_API_BASE_URL=

# Layer 1 Terminal (orderflow/orderbook backend)
# Used by Next.js API proxies under /api/layer1/*
# Default is http://127.0.0.1:8000 when not set.
LAYER1_API_BASE_URL=

# Admin Authentication (optional)
NEXTAUTH_SECRET=
NEXTAUTH_URL=
ADMIN_EMAIL=
ADMIN_PASSWORD=

# Donation wallet balance RPC (optional)
BSC_RPC_URL=

# User Authentication (community accounts)
# Required for /api/auth/login to issue HTTP-only session cookies
SESSION_SECRET=

# Password reset emails (Brevo SMTP - optional)
# If not set, the app will log reset links to the server console.
NEXT_PUBLIC_SITE_URL=
MAIL_FROM=
SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# Example environment variables
# Copy this file to .env.local and fill in your API keys

# Optional: CoinMarketCap API (for additional data)
# NEXT_PUBLIC_CMC_API_KEY=your_api_key_here

# Optional: Glassnode API (for on-chain data)
# NEXT_PUBLIC_GLASSNODE_API_KEY=your_api_key_here

# Optional: Messari API (for research & metrics)
# NEXT_PUBLIC_MESSARI_API_KEY=your_api_key_here

# Optional: Covalent API (for transaction data and whale alerts)
# Get free API key at: https://www.covalenthq.com/platform/
# COVALENT_API_KEY=your_covalent_api_key_here

# Optional: DeBank API (for portfolio data - may have rate limits)
# DEBANK_API_KEY=your_debank_api_key_here

# Optional: Dune Analytics (for multi-chain DeFi transactions)
# Get free API key at: https://dune.com/account
# Note: Requires saved queries for execution - free tier has limitations
# DUNE_API_KEY=your_dune_api_key_here

# Optional: Flipside Crypto (for advanced blockchain analytics)
# Get free API key at: https://flipsidecrypto.xyz/
# FLIPSIDE_API_KEY=your_flipside_api_key_here

# Optional: Etherscan API (fallback for Ethereum transaction data)
# Get free API key at: https://etherscan.io/apis (recommended for better rate limits)
# ETHERSCAN_API_KEY=YourApiKeyToken

# Optional: MongoDB URI (for database access)
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/<dbname>?retryWrites=true&w=majority

# Optional: Imgbb API Key (for image uploads)
# imgbb_api_key=your_imgbb_api_key_here